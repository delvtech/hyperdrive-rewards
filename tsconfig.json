{
  "compilerOptions": {
    "target": "ESNext",
    "module": "NodeNext",
    "outDir": "./dist",
    "rootDir": ".",
    "baseUrl": ".",
    "strict": true,
    "esModuleInterop": true,
    // This is a workaround for a bug in the webautn-p256 package, which was
    // giving this error:
    //
    // node_modules/webauthn-p256/_types/types.d.ts:27:67 - error TS2536:
    // Type '_K' cannot be used to index type 'fallback'.
    //
    // See this github issue for more details:
    // https://github.com/wevm/viem/issues/2621
    "skipLibCheck": true,
    // These are needed to allow typeorm decorators to work.
    "emitDecoratorMetadata": true,
    "experimentalDecorators": true,
    // This must be disabled for entity properties to be defined.  Without this
    // the entity properties need to have a default value set in the class
    // constructor.
    "strictPropertyInitialization": false,
    "forceConsistentCasingInFileNames": true,
    "paths": {
      "src/*": ["./src/*"]
    }
  },
  "exclude": ["lib/**/*"],
  "include": ["./src", "./hardhat.config.ts"]
}
